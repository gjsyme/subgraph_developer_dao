{
  "name": "developer_dao",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ developer_dao",
    "create-local": "graph create --node http://localhost:8020/ developer_dao",
    "remove-local": "graph remove --node http://localhost:8020/ developer_dao",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 developer_dao"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.22.0",
    "@graphprotocol/graph-ts": "0.22.0"
  }
}
